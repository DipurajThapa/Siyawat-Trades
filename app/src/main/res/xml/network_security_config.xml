<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Deny all cleartext HTTP traffic across the entire application -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Only trust pre-installed Android system CAs; reject user-installed CAs to thwart MitM proxies -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Specific domain rules for Google Drive / Google APIs with TLS 1.3 -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">google.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
